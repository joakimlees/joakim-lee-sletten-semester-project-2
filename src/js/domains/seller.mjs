// class for seller object
export class sellerObject {
  constructor(name, email, avatar, wins) {
    this.username = name;
    this.email = email;
    this.avatar = avatar;
    this.wins = wins;
  }
}
